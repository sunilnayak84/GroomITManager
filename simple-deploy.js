/**
 * SIMPLE PRODUCTION DEPLOYMENT SCRIPT
 * 
 * This is a lightweight version that uses existing builds and avoids timeouts
 */

import './production-deploy.js';

console.log('Simple deployment script loaded - using production-deploy.js with existing build');